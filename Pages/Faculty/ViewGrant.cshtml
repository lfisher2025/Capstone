@page
@model Lab1.Pages.Faculty.ViewGrantModel
@{
    ViewData["Title"] = "View Grants";
}

<h2>Grants</h2>



@{ //Used ChatGPT to troubleshoot my switch case (Lines 30-37)
    string GetStatusClass(string status)
    {
        return status switch
        {
            "Applied" => "text-primary",
            "Pending" => "text-warning",
            "Approved" => "text-success",
            "Rejected" => "text-danger",
            _ => "text-dark"
        };
    }
}

<table class="table table-striped table-striped-columns table-bordered">
    <thead>
        <tr>
            <th>Grant Name</th>
            <th>Funding Agency</th>
            <th>Submission Date</th>
            <th>Deadline</th>
            <th>Funding Amount</th>
            <th>Type</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var grant in Model.GrantInfo)
        {
            <tr>
                <td>@grant.GrantName</td>
                <td>@grant.FundingAgency</td>
                <td>@grant.SubmissionDate.ToString("yyyy-MM-dd")</td>
                <td>@grant.Deadline.ToString("yyyy-MM-dd")</td>
                <td>@grant.FundingAmount.ToString("C")</td>
                <td>@grant.Type</td>
                <td>@grant.GrantDescription</td>
                <td><a asp-page="/Faculty/EditGrant" class="btn btn-primary">Edit</a></td>
            </tr>
        }
    </tbody>
</table>

<a asp-page="/Faculty/AddGrant" class="btn btn-primary">Add Grant</a>
<a asp-page="/Faculty/GrantSearch" class="btn btn-secondary">Search Archived Grants</a>